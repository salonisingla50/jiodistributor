'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await res.json();if(res.ok){localStorage.setItem('admin','true');r.push('/admin')}else setMsg(d.message)}return <main style={{minHeight:"75vh",display:"grid",placeItems:"center",padding:30}}><form onSubmit={go} className="card" style={{width:"100%",maxWidth:440,padding:35}}><h1 style={{fontSize:35}}>Welcome back</h1><p style={{color:"#667085"}}>Sign in to manage the website.</p><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} required style={{margin:"7px 0 18px"}}/><label>Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required style={{margin:"7px 0 18px"}}/>{msg&&<p style={{color:"#d92d20"}}>{msg}</p>}<button className="btn primary" style={{width:"100%"}}>Sign in</button><div style={{background:"#f4f7ff",padding:15,borderRadius:12,marginTop:18,fontSize:13}}>Demo admin: admin@jio-demo.local / admin123</div><Link href="/" style={{display:"block",textAlign:"center",marginTop:20,color:"#0757ff"}}>Back to website</Link></form></main>}